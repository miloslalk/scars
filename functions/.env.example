# Copy to functions/.env before deploying. Values are loaded into
# process.env by the Firebase CLI (replaces the retired functions.config()).
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MODERATION_EMAIL=moderator@example.com
MODERATION_BASE_URL=https://europe-west1-scars-d81cd.cloudfunctions.net
MODERATION_FROM=Scars App <no-reply@scars.app>
